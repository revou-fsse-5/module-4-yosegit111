{
  "name": "your-project-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd registration-form && npm start",
    "build": "cd registration-form && npm run build",
    "predeploy": "cd registration-form && npm run build",
    "deploy": "cp -r registration-form/build/* . && git add . && git commit -m 'Deploy build' && git push origin main"
  },
  "homepage": "http://revou-fsse-5.github.io/module-4-yosegit111"
}
